🏗 STEP 1 — Create React Project
npm create vite@latest pickleclub
cd pickleclub
npm install


Choose:

React

JavaScript (or TypeScript if you prefer)

🎨 STEP 2 — Install Tailwind

Install Tailwind:

npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p


Open tailwind.config.js and replace content with:

export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

🧵 STEP 3 — Add Poppins Font

In index.html:

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">


Then in src/index.css:

@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  font-family: 'Poppins', sans-serif;
}

🏠 STEP 4 — Build the Modern Homepage

Replace App.jsx with this starter layout:

export default function App() {
  return (
    <div className="bg-gray-50 min-h-screen text-gray-900">

      {/* Navbar */}
      <nav className="bg-white shadow-sm sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
          <h1 className="text-xl font-semibold">PickleClub</h1>
          <div className="space-x-6 text-sm font-medium">
            <a href="#" className="hover:text-blue-500">Home</a>
            <a href="#" className="hover:text-blue-500">Members</a>
            <a href="#" className="hover:text-blue-500">Rankings</a>
            <a href="#" className="hover:text-blue-500">Schedule</a>
            <a href="#" className="hover:text-blue-500">Tournaments</a>
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <section className="max-w-7xl mx-auto px-6 py-20 flex flex-col md:flex-row items-center justify-between">
        <div className="max-w-xl">
          <h2 className="text-5xl font-bold mb-6">
            Elevate Your Game.
          </h2>
          <p className="text-gray-500 mb-8">
            Track rankings, schedules, and tournaments in one modern platform.
          </p>
          <div className="space-x-4">
            <button className="bg-blue-500 text-white px-6 py-3 rounded-xl shadow hover:bg-blue-600 transition">
              View Rankings
            </button>
            <button className="border border-gray-300 px-6 py-3 rounded-xl hover:bg-gray-100 transition">
              View Tournaments
            </button>
          </div>
        </div>

        {/* Right decorative area */}
        <div className="mt-12 md:mt-0 w-96 h-64 bg-gradient-to-r from-blue-100 to-blue-50 rounded-3xl shadow-inner"></div>
      </section>

      {/* Stats Section */}
      <section className="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-4 gap-6 pb-20">
        <div className="bg-white p-6 rounded-2xl shadow hover:shadow-md transition">
          <h3 className="text-3xl font-bold">128</h3>
          <p className="text-gray-500">Members</p>
        </div>
        <div className="bg-white p-6 rounded-2xl shadow hover:shadow-md transition">
          <h3 className="text-3xl font-bold">01</h3>
          <p className="text-gray-500">Top Player</p>
        </div>
        <div className="bg-white p-6 rounded-2xl shadow hover:shadow-md transition">
          <h3 className="text-3xl font-bold">04</h3>
          <p className="text-gray-500">Upcoming Matches</p>
        </div>
        <div className="bg-white p-6 rounded-2xl shadow hover:shadow-md transition">
          <h3 className="text-3xl font-bold">02</h3>
          <p className="text-gray-500">Tournaments</p>
        </div>
      </section>

    </div>
  )
}

🚀 STEP 5 — Run It
npm run dev


You now have:

Modern navbar

Clean hero section

Soft gradient

Rounded aesthetic cards

Poppins typography

Smooth hover effects

It already looks modern.

✨ To Make It EXACTLY Like the Image

Next upgrades:

Install Lucide icons:

npm install lucide-react


Add subtle card borders:

border border-gray-100


Use softer shadows:

shadow-sm hover:shadow-lg


Add Framer Motion for smooth animations:

npm install framer-motion